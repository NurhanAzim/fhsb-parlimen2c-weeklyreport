services:
  weekly-report-bot:
    build: .
    container_name: weekly-report-bot-staging
    restart: unless-stopped
    env_file:
      - .env.staging
    volumes:
      - ./data-staging:/app/data-staging
      - ./runtime-staging:/app/runtime-staging
